# Шпаргалка к коллоквиуму по структурам данных и алгоритмам

## 1. Оценка сложности алгоритма по времени

### Основные классы сложности (от быстрого к медленному):
- **O(1)** — константная сложность (операция выполняется за фиксированное время)
- **O(log n)** — логарифмическая (бинарный поиск)
- **O(n)** — линейная (один цикл по массиву)
- **O(n log n)** — линейно-логарифмическая (сортировка слиянием)
- **O(n²)** — квадратичная (вложенные циклы)
- **O(n³)** — кубическая (три вложенных цикла)
- **O(2ⁿ)** — экспоненциальная (перебор всех подмножеств)
- **O(n!)** — факториальная (перебор перестановок)

### Правила подсчёта:
1. Опускаем константные множители: O(5n) = O(n)
2. Опускаем старшие члены: O(n² + n) = O(n²)
3. При сложении циклов — берём максимум: O(n) + O(n²) = O(n²)
4. При вложении циклов — умножаем: O(n) × O(n) = O(n²)

---

## 2. Оценка сложности алгоритма по памяти

### Пространственная сложность:
Показывает, сколько дополнительной памяти требует алгоритм относительно размера входных данных.

- **O(1)** — константная память (несколько переменных)
- **O(n)** — линейная (создаём массив размера n)
- **O(log n)** — логарифмическая (глубина рекурсии)
- **O(n²)** — квадратичная (двумерный массив n×n)

### Важно:
- Память входных данных НЕ считается в O-нотации
- Считаем только ДОПОЛНИТЕЛЬНУЮ память
- Для рекурсии — глубина стека вызовов

---

## 3. Сортировка вставками (Insertion Sort)

### Идея:
Проходим по массиву слева направо. Для каждого элемента вставляем его на правильное место в уже отсортированную левую часть.

### Сложность:
- **Время**: O(n²) в среднем/худшем; O(n) в лучшем (уже отсортирован)
- **Память**: O(1)
- **Устойчивость**: ДА

### Код:
```cpp
void insertionSort(int arr[], int n) {
    for (int i = 1; i < n; i++) {
        int key = arr[i];
        int j = i - 1;
        
        // Сдвигаем элементы больше key на 1 позицию вправо
        while (j >= 0 && arr[j] > key) {
            arr[j + 1] = arr[j];
            j--;
        }
        arr[j + 1] = key;
    }
}
```

### Когда использовать:
- Малые массивы (n < 50)
- Почти отсортированные данные
- Нужна устойчивость

---

## 4. Сортировка слиянием (Merge Sort)

### Идея:
Рекурсивно делим массив пополам, сортируем две половины и сливаем.

### Сложность:
- **Время**: O(n log n) всегда (даже в худшем случае!)
- **Память**: O(n)
- **Устойчивость**: ДА

### Код:
```cpp
void merge(int arr[], int left, int mid, int right) {
    int n1 = mid - left + 1;
    int n2 = right - mid;
    
    int* L = new int[n1];
    int* R = new int[n2];
    
    for (int i = 0; i < n1; i++) L[i] = arr[left + i];
    for (int i = 0; i < n2; i++) R[i] = arr[mid + 1 + i];
    
    int i = 0, j = 0, k = left;
    
    while (i < n1 && j < n2) {
        if (L[i] <= R[j]) {
            arr[k++] = L[i++];
        } else {
            arr[k++] = R[j++];
        }
    }
    
    while (i < n1) arr[k++] = L[i++];
    while (j < n2) arr[k++] = R[j++];
    
    delete[] L;
    delete[] R;
}

void mergeSort(int arr[], int left, int right) {
    if (left < right) {
        int mid = left + (right - left) / 2;
        mergeSort(arr, left, mid);
        mergeSort(arr, mid + 1, right);
        merge(arr, left, mid, right);
    }
}
```

### Преимущества:
- Гарантированная O(n log n)
- Устойчивая сортировка
- Хорошо работает с внешней памятью

### Недостатки:
- Требует O(n) доп. памяти
- Медленнее quicksort на практике

---

## 5. Быстрая сортировка (Quick Sort)

### Идея:
Выбираем pivot, разбиваем массив на элементы < pivot и > pivot, рекурсивно сортируем две части.

### Сложность:
- **Время**: O(n log n) в среднем; **O(n²) в худшем**
- **Память**: O(log n) в среднем
- **Устойчивость**: НЕТ (обычно)

### Код:
```cpp
int partition(int arr[], int low, int high) {
    int pivot = arr[high];
    int i = low - 1;
    
    for (int j = low; j < high; j++) {
        if (arr[j] < pivot) {
            i++;
            swap(arr[i], arr[j]);
        }
    }
    swap(arr[i + 1], arr[high]);
    return i + 1;
}

void quickSort(int arr[], int low, int high) {
    if (low < high) {
        int pi = partition(arr, low, high);
        quickSort(arr, low, pi - 1);
        quickSort(arr, pi + 1, high);
    }
}
```

### Оптимизация — выбор pivot:
- Медиана из трёх: `median3(arr[low], arr[mid], arr[high])`
- Случайный элемент: помогает избежать O(n²) на отсортированных данных

### Когда использовать:
- Быстро на практике благодаря кешу
- Не нужна доп. память
- Средняя сложность O(n log n)

---

## 6. Сортировка подсчетом (Counting Sort)

### Идея:
Если элементы — целые числа в диапазоне [0, k], то:
1. Считаем количество каждого числа
2. Восстанавливаем отсортированный массив

### Сложность:
- **Время**: O(n + k), где k — диапазон значений
- **Память**: O(k)
- **Устойчивость**: ДА

### Код:
```cpp
void countingSort(int arr[], int n, int maxVal) {
    vector<int> count(maxVal + 1, 0);
    
    // Считаем
    for (int i = 0; i < n; i++) {
        count[arr[i]]++;
    }
    
    // Восстанавливаем
    int idx = 0;
    for (int i = 0; i <= maxVal; i++) {
        for (int j = 0; j < count[i]; j++) {
            arr[idx++] = i;
        }
    }
}

// Устойчивая версия:
void countingSortStable(int arr[], int n, int maxVal) {
    vector<int> count(maxVal + 1, 0);
    vector<int> output(n);
    
    for (int i = 0; i < n; i++) count[arr[i]]++;
    for (int i = 1; i <= maxVal; i++) count[i] += count[i - 1];
    
    for (int i = n - 1; i >= 0; i--) {
        output[count[arr[i]] - 1] = arr[i];
        count[arr[i]]--;
    }
    
    for (int i = 0; i < n; i++) arr[i] = output[i];
}
```

### Когда использовать:
- Целые числа в известном диапазоне
- k ≤ n (иначе неэффективно)
- Нужна устойчивость

---

## 7. Цифровая сортировка (Radix Sort)

### Идея:
Сортируем по одной цифре/байту за раз, начиная с младшей цифры (LSD).
Для каждого прохода используем counting sort.

### Сложность:
- **Время**: O(d × (n + k)), где d — количество цифр, k — основание системы
- Если d = О(1), то **O(n)**
- **Память**: O(n + k)
- **Устойчивость**: ДА

### Код:
```cpp
void radixSort(int arr[], int n) {
    int maxVal = *max_element(arr, arr + n);
    
    // Сортируем по каждой цифре
    for (int exp = 1; maxVal / exp > 0; exp *= 10) {
        countingSortByDigit(arr, n, exp);
    }
}

void countingSortByDigit(int arr[], int n, int exp) {
    vector<int> output(n);
    vector<int> count(10, 0);
    
    // Считаем по цифре
    for (int i = 0; i < n; i++) {
        count[(arr[i] / exp) % 10]++;
    }
    
    // Позиции в output
    for (int i = 1; i < 10; i++) {
        count[i] += count[i - 1];
    }
    
    // Строим output (с конца, для устойчивости)
    for (int i = n - 1; i >= 0; i--) {
        int digit = (arr[i] / exp) % 10;
        output[count[digit] - 1] = arr[i];
        count[digit]--;
    }
    
    for (int i = 0; i < n; i++) arr[i] = output[i];
}
```

### Преимущества:
- Линейная сложность O(n) при d = O(1)
- Устойчивая
- Хорошо работает с большими числами

---

## 8. Стек (Stack)

### Определение:
LIFO (Last In First Out) — последний вошёл, первый вышел.

### Операции:
- **push(x)** — добавить элемент на верх
- **pop()** — удалить верхний элемент
- **top()** — получить верхний элемент
- **isEmpty()** — проверка пустоты
- **size()** — размер

### На массиве:
```cpp
class Stack {
private:
    int* arr;
    int top;
    int capacity;
    
public:
    Stack(int cap) {
        capacity = cap;
        arr = new int[capacity];
        top = -1;
    }
    
    void push(int x) {
        if (top < capacity - 1) {
            arr[++top] = x;
        }
    }
    
    int pop() {
        if (top >= 0) {
            return arr[top--];
        }
        return -1;
    }
    
    int getTop() {
        if (top >= 0) return arr[top];
        return -1;
    }
    
    bool isEmpty() {
        return top == -1;
    }
    
    ~Stack() {
        delete[] arr;
    }
};
```

### Применение:
- Проверка скобок
- Обратная польская нотация
- Поиск в глубину (DFS)
- Отмена действий (Ctrl+Z)

---

## 9. Очередь (Queue)

### Определение:
FIFO (First In First Out) — первый вошёл, первый вышел.

### Операции:
- **enqueue(x)** — добавить в конец
- **dequeue()** — удалить из начала
- **front()** — получить первый элемент
- **isEmpty()** — проверка пустоты

### На массиве (циклическая):
```cpp
class Queue {
private:
    int* arr;
    int front;
    int rear;
    int capacity;
    int count;
    
public:
    Queue(int cap) {
        capacity = cap;
        arr = new int[capacity];
        front = 0;
        rear = -1;
        count = 0;
    }
    
    void enqueue(int x) {
        if (count < capacity) {
            rear = (rear + 1) % capacity;
            arr[rear] = x;
            count++;
        }
    }
    
    int dequeue() {
        if (count > 0) {
            int x = arr[front];
            front = (front + 1) % capacity;
            count--;
            return x;
        }
        return -1;
    }
    
    bool isEmpty() {
        return count == 0;
    }
    
    ~Queue() {
        delete[] arr;
    }
};
```

### Применение:
- BFS (поиск в ширину)
- Очередь задач
- Буферизация

---

## 10. Односвязный список (Singly Linked List)

### Структура узла:
```cpp
struct Node {
    int data;
    Node* next;
    Node(int x) : data(x), next(nullptr) {}
};
```

### Основные операции:
```cpp
class LinkedList {
private:
    Node* head;
    
public:
    LinkedList() : head(nullptr) {}
    
    // Вставка в начало
    void pushFront(int x) {
        Node* newNode = new Node(x);
        newNode->next = head;
        head = newNode;
    }
    
    // Вставка в конец
    void pushBack(int x) {
        Node* newNode = new Node(x);
        if (head == nullptr) {
            head = newNode;
            return;
        }
        Node* current = head;
        while (current->next != nullptr) {
            current = current->next;
        }
        current->next = newNode;
    }
    
    // Удаление с начала
    void popFront() {
        if (head != nullptr) {
            Node* temp = head;
            head = head->next;
            delete temp;
        }
    }
    
    // Поиск элемента
    Node* search(int x) {
        Node* current = head;
        while (current != nullptr) {
            if (current->data == x) return current;
            current = current->next;
        }
        return nullptr;
    }
    
    // Вывод
    void display() {
        Node* current = head;
        while (current != nullptr) {
            cout << current->data << " -> ";
            current = current->next;
        }
        cout << "null" << endl;
    }
};
```

### Характеристики:
- **Поиск**: O(n)
- **Вставка/удаление в начало**: O(1)
- **Вставка/удаление в конец**: O(n)

---

## 11. Двусвязный список (Doubly Linked List)

### Структура узла:
```cpp
struct Node {
    int data;
    Node* next;
    Node* prev;
    Node(int x) : data(x), next(nullptr), prev(nullptr) {}
};
```

### Дополнительные операции по сравнению с односвязным:
```cpp
class DoublyLinkedList {
private:
    Node* head;
    
public:
    DoublyLinkedList() : head(nullptr) {}
    
    void pushBack(int x) {
        Node* newNode = new Node(x);
        if (head == nullptr) {
            head = newNode;
            return;
        }
        Node* current = head;
        while (current->next != nullptr) {
            current = current->next;
        }
        current->next = newNode;
        newNode->prev = current;
    }
    
    // Удаление произвольного элемента (знаем узел)
    void deleteNode(Node* node) {
        if (node->prev != nullptr) {
            node->prev->next = node->next;
        } else {
            head = node->next;
        }
        if (node->next != nullptr) {
            node->next->prev = node->prev;
        }
        delete node;
    }
    
    // Обход в обратном порядке
    void displayReverse() {
        Node* current = head;
        while (current->next != nullptr) {
            current = current->next;
        }
        while (current != nullptr) {
            cout << current->data << " <- ";
            current = current->prev;
        }
        cout << "null" << endl;
    }
};
```

### Преимущества:
- Быстрое удаление элемента (O(1), если известен узел)
- Обход в обе стороны
- Легко реализовать операции типа "перейти на предыдущий"

### Недостатки:
- Больше памяти (два указателя вместо одного)

---

## 12. Циклический список (Circular Linked List)

### Идея:
Последний узел указывает на первый, образуя кольцо.

### Структура:
```cpp
class CircularLinkedList {
private:
    Node* head;
    
public:
    CircularLinkedList() : head(nullptr) {}
    
    void pushBack(int x) {
        Node* newNode = new Node(x);
        if (head == nullptr) {
            head = newNode;
            head->next = head; // Указывает на себя
            return;
        }
        Node* current = head;
        while (current->next != head) { // Пока не найдём последний
            current = current->next;
        }
        current->next = newNode;
        newNode->next = head;
    }
    
    void display() {
        if (head == nullptr) return;
        Node* current = head;
        do {
            cout << current->data << " -> ";
            current = current->next;
        } while (current != head);
        cout << "(back to head)" << endl;
    }
};
```

### Применение:
- Круговые буферы
- Симуляция игр в круг (музыкальные стулья)
- Справедливое распределение ресурсов

---

## 13. Стек на списках

### Реализация:
```cpp
class StackList {
private:
    Node* top;
    
public:
    StackList() : top(nullptr) {}
    
    void push(int x) {
        Node* newNode = new Node(x);
        newNode->next = top;
        top = newNode;
    }
    
    int pop() {
        if (top != nullptr) {
            int x = top->data;
            Node* temp = top;
            top = top->next;
            delete temp;
            return x;
        }
        return -1;
    }
    
    int getTop() {
        if (top != nullptr) return top->data;
        return -1;
    }
    
    bool isEmpty() {
        return top == nullptr;
    }
    
    ~StackList() {
        while (top != nullptr) pop();
    }
};
```

### Преимущества над массивом:
- Динамический размер
- Нет ограничения по размеру
- O(1) для всех операций

---

## 14. Очередь на списках

### Реализация:
```cpp
class QueueList {
private:
    Node* front;
    Node* rear;
    
public:
    QueueList() : front(nullptr), rear(nullptr) {}
    
    void enqueue(int x) {
        Node* newNode = new Node(x);
        if (rear == nullptr) {
            front = rear = newNode;
        } else {
            rear->next = newNode;
            rear = newNode;
        }
    }
    
    int dequeue() {
        if (front != nullptr) {
            int x = front->data;
            Node* temp = front;
            front = front->next;
            if (front == nullptr) rear = nullptr;
            delete temp;
            return x;
        }
        return -1;
    }
    
    bool isEmpty() {
        return front == nullptr;
    }
    
    ~QueueList() {
        while (front != nullptr) dequeue();
    }
};
```

### Сложность:
- **Enqueue**: O(1)
- **Dequeue**: O(1)
- **Память**: O(n)

---

## 15. Пирамида (Heap)

### Определение:
Полное бинарное дерево, где каждый родитель ≥ (макс-пирамида) или ≤ (мин-пирамида) своих детей.

### Для элемента с индексом i:
- Левый ребёнок: 2i + 1
- Правый ребёнок: 2i + 2
- Родитель: (i - 1) / 2

### Восстановление свойств (heapify):

**Просеивание вниз (siftDown)** — элемент слишком мал:
```cpp
void siftDown(int arr[], int n, int i) {
    int smallest = i;
    int left = 2 * i + 1;
    int right = 2 * i + 2;
    
    if (left < n && arr[left] < arr[smallest]) {
        smallest = left;
    }
    if (right < n && arr[right] < arr[smallest]) {
        smallest = right;
    }
    
    if (smallest != i) {
        swap(arr[i], arr[smallest]);
        siftDown(arr, n, smallest);
    }
}
```

**Просеивание вверх (siftUp)** — элемент слишком велик:
```cpp
void siftUp(int arr[], int i) {
    while (i > 0 && arr[(i - 1) / 2] > arr[i]) {
        swap(arr[i], arr[(i - 1) / 2]);
        i = (i - 1) / 2;
    }
}
```

### Вставка элемента:
```cpp
void insert(int arr[], int& n, int x) {
    arr[n] = x;
    siftUp(arr, n);
    n++;
}
```

### Удаление минимума:
```cpp
int deleteMin(int arr[], int& n) {
    int min = arr[0];
    arr[0] = arr[n - 1];
    n--;
    siftDown(arr, n, 0);
    return min;
}
```

### Операции:
- **Вставка**: O(log n)
- **Удаление минимума**: O(log n)
- **Поиск минимума**: O(1)

---

## 16. Построение пирамиды O(n)

### Неправильно (O(n log n)):
```cpp
for (int i = 0; i < n; i++) {
    siftUp(arr, i);  // O(log n) для каждого
}
// Итого: O(n log n)
```

### Правильно (O(n)):
```cpp
void buildHeap(int arr[], int n) {
    // Начинаем с последнего узла с детьми
    for (int i = n / 2 - 1; i >= 0; i--) {
        siftDown(arr, n, i);
    }
}
```

### Почему O(n)?
На каждом уровне количество операций убывает. Количество узлов на уровне k: n/2^k. Стоимость siftDown на уровне k: k. Сумма = n × (1/2 × 1 + 1/4 × 2 + 1/8 × 3 + ...) = n (геометрический ряд).

---

## 17. Пирамидальная сортировка (Heap Sort)

### Алгоритм:
1. Построить пирамиду из массива
2. Извлекать минимум n раз
    
### Код:
```cpp
void heapSort(int arr[], int n) {
    // Построение пирамиды
    for (int i = n / 2 - 1; i >= 0; i--) {
        siftDown(arr, n, i);
    }
    
    // Извлечение элементов
    for (int i = n - 1; i > 0; i--) {
        swap(arr[0], arr[i]);
        siftDown(arr, i, 0);
    }
}
```

### Сложность:
- **Время**: O(n log n) всегда
- **Память**: O(1)
- **Устойчивость**: НЕТ

### vs Quick Sort:
- Heap Sort: гарантированно O(n log n), но медленнее на практике
- Quick Sort: быстрее в среднем, но может быть O(n²)

---

## 18. Удаление элемента из пирамиды

### Удаление произвольного элемента (по индексу):
```cpp
void deleteElement(int arr[], int& n, int i) {
    if (i < 0 || i >= n) return;
    
    arr[i] = arr[n - 1];
    n--;
    
    if (i < n) {
        // Проверяем в обе стороны
        if (i > 0 && arr[i] < arr[(i - 1) / 2]) {
            siftUp(arr, i);
        } else {
            siftDown(arr, n, i);
        }
    }
}
```

### Удаление корня (минимума/максимума):
```cpp
int deleteRoot(int arr[], int& n) {
    int root = arr[0];
    arr[0] = arr[n - 1];
    n--;
    siftDown(arr, n, 0);
    return root;
}
```

### Сложность:
- **Удаление**: O(log n)

---

## 19. Очередь с приоритетом (Priority Queue)

### На основе пирамиды:
```cpp
class PriorityQueue {
private:
    int* arr;
    int size;
    int capacity;
    
public:
    PriorityQueue(int cap) {
        capacity = cap;
        arr = new int[capacity];
        size = 0;
    }
    
    // Вставка элемента
    void insert(int x) {
        if (size < capacity) {
            arr[size] = x;
            siftUp(arr, size);
            size++;
        }
    }
    
    // Получить элемент с максимальным приоритетом
    int getMax() {
        return arr[0];
    }
    
    // Удалить элемент с максимальным приоритетом
    int extractMax() {
        int max = arr[0];
        arr[0] = arr[size - 1];
        size--;
        siftDown(arr, size, 0);
        return max;
    }
    
    // Повышение приоритета элемента
    void increaseKey(int i, int newVal) {
        if (newVal < arr[i]) return; // Не понизиваем
        arr[i] = newVal;
        siftUp(arr, i);
    }
    
    // Понижение приоритета элемента
    void decreaseKey(int i, int newVal) {
        if (newVal > arr[i]) return; // Не повышаем
        arr[i] = newVal;
        siftDown(arr, size, i);
    }
    
    bool isEmpty() {
        return size == 0;
    }
};
```

### Применение:
- Алгоритм Дейкстры (кратчайший путь)
- Алгоритм Прима (минимальное остовное дерево)
- Планирование задач
- Медиана в потоке данных

### Сложность:
- **insert**: O(log n)
- **extractMax**: O(log n)
- **increaseKey/decreaseKey**: O(log n)

---

## 20. Динамическое программирование (DP)

### Базовый принцип:
Разбиваем задачу на подзадачи, решаем их один раз и запоминаем результаты.

**Два подхода:**
1. **Memoization** (сверху вниз) — рекурсия с кешем
2. **Tabulation** (снизу вверх) — заполнение таблицы

---

### 20.1 Кузнечик (Frog Jump)

**Задача:** Кузнечик на камне 0 может прыгать на 1 или 2 камня вперёд. Сколько способов достичь камень n?

**Рекуррентность:** f(n) = f(n-1) + f(n-2)

**Memoization (рекурсия + кеш):**
```cpp
map<int, long long> memo;

long long frog(int n) {
    if (n <= 1) return 1;
    if (memo.find(n) != memo.end()) return memo[n];
    
    memo[n] = frog(n - 1) + frog(n - 2);
    return memo[n];
}
```

**Tabulation (итеративно):**
```cpp
long long frogTab(int n) {
    if (n <= 1) return 1;
    vector<long long> dp(n + 1);
    dp[0] = 1;
    dp[1] = 1;
    
    for (int i = 2; i <= n; i++) {
        dp[i] = dp[i - 1] + dp[i - 2];
    }
    return dp[n];
}
```

**Оптимизация по памяти:**
```cpp
long long frogOptimized(int n) {
    if (n <= 1) return 1;
    long long a = 1, b = 1;
    
    for (int i = 2; i <= n; i++) {
        long long c = a + b;
        a = b;
        b = c;
    }
    return b;
}
```

**Сложность:**
- **Время**: O(n)
- **Память**: O(1) или O(n) в зависимости от подхода

---

### 20.2 Рюкзак (Knapsack)

**Задача:** У нас есть рюкзак вместимостью W и n предметов с весами w[i] и стоимостью v[i]. Максимизировать стоимость.

**Рекуррентность:** 
```
dp[i][w] = max(dp[i-1][w], dp[i-1][w-w[i]] + v[i])
```

**0/1 Knapsack (каждый предмет один раз):**
```cpp
int knapsack(int W, int w[], int v[], int n) {
    vector<vector<int>> dp(n + 1, vector<int>(W + 1, 0));
    
    for (int i = 1; i <= n; i++) {
        for (int j = 0; j <= W; j++) {
            if (w[i - 1] <= j) {
                // Либо берём, либо не берём
                dp[i][j] = max(
                    dp[i - 1][j],                          // Не берём
                    dp[i - 1][j - w[i - 1]] + v[i - 1]    // Берём
                );
            } else {
                dp[i][j] = dp[i - 1][j];
            }
        }
    }
    return dp[n][W];
}
```

**Оптимизация по памяти (1D array):**
```cpp
int knapsackOptimized(int W, int w[], int v[], int n) {
    vector<int> dp(W + 1, 0);
    
    for (int i = 0; i < n; i++) {
        for (int j = W; j >= w[i]; j--) {
            dp[j] = max(dp[j], dp[j - w[i]] + v[i]);
        }
    }
    return dp[W];
}
```

**Сложность:**
- **Время**: O(n × W)
- **Память**: O(W)

---

### 20.3 НВП (Longest Increasing Subsequence)

**Задача:** Найти длину самой длинной возрастающей подпоследовательности.

**Пример:** [3, 10, 2, 1, 20] → НВП = [3, 10, 20], длина = 3

**Рекуррентность:**
```
dp[i] = максимум(dp[j] + 1) для всех j < i где arr[j] < arr[i]
```

**O(n²) решение:**
```cpp
int longestIncreasingSubsequence(int arr[], int n) {
    vector<int> dp(n, 1); // Каждый элемент — подпоследовательность длины 1
    
    for (int i = 1; i < n; i++) {
        for (int j = 0; j < i; j++) {
            if (arr[j] < arr[i]) {
                dp[i] = max(dp[i], dp[j] + 1);
            }
        }
    }
    
    return *max_element(dp.begin(), dp.end());
}
```

**O(n log n) решение (с бинарным поиском):**
```cpp
int longestIncreasingSubsequenceFast(int arr[], int n) {
    vector<int> lis; // lis[i] = минимальный конец подпоследовательности длины i+1
    
    for (int i = 0; i < n; i++) {
        auto it = lower_bound(lis.begin(), lis.end(), arr[i]);
        if (it == lis.end()) {
            lis.push_back(arr[i]);
        } else {
            *it = arr[i];
        }
    }
    
    return lis.size();
}
```

**Восстановление самой последовательности:**
```cpp
vector<int> reconstructLIS(int arr[], int n) {
    vector<int> dp(n, 1);
    vector<int> parent(n, -1);
    
    for (int i = 1; i < n; i++) {
        for (int j = 0; j < i; j++) {
            if (arr[j] < arr[i] && dp[j] + 1 > dp[i]) {
                dp[i] = dp[j] + 1;
                parent[i] = j;
            }
        }
    }
    
    int maxIdx = max_element(dp.begin(), dp.end()) - dp.begin();
    vector<int> lis;
    
    for (int i = maxIdx; i != -1; i = parent[i]) {
        lis.push_back(arr[i]);
    }
    
    reverse(lis.begin(), lis.end());
    return lis;
}
```

**Сложность:**
- **O(n²) решение**: O(n²)
- **O(n log n) решение**: O(n log n)

---

## Шпаргалка по формулам

| Понятие | Формула / Определение |
|---------|------------------------|
| **O-нотация** | Асимптотическая верхняя граница |
| **Ω-нотация** | Асимптотическая нижняя граница |
| **Θ-нотация** | Точная оценка (O и Ω одновременно) |
| **log₂ n** | Основание 2 (для анализа алгоритмов) |
| **n log n** | Типично для сортировок: mergesort, quicksort (avg) |
| **Геометрический ряд** | 1 + 2 + 4 + ... + 2^(k-1) = 2^k - 1 |
| **Гармонический ряд** | 1 + 1/2 + 1/3 + ... + 1/n ≈ ln(n) |

---

## Частые ошибки

1. **Путаница O и Ω**: O(n²) ≠ Ω(n²). O — верхняя граница, Ω — нижняя.
2. **Забывают про память**: Mergesort O(n log n), но O(n) памяти!
3. **Quicksort худший случай**: На отсортированном массиве может быть O(n²), используйте random pivot.
4. **Heap sort не устойчива**: Если нужна устойчивость, используйте mergesort.
5. **Radix sort не универсальная**: Работает только для целых чисел/строк.
6. **DP vs Greedy**: Greedy не всегда работает! Knapsack требует DP.

---

## Проверочные вопросы на коллоквиум

1. Какая сложность у insertion sort в лучшем случае? **O(n)**
2. Merge sort работает за O(n log n). Какая память? **O(n)**
3. Heap sort — O(n log n). Устойчива ли? **НЕТ**
4. Quick sort в худшем случае? **O(n²)**, как исправить? Случайный pivot или медиана
5. Counting sort когда использовать? Когда k ≤ n и целые числа
6. Radix sort сложность? **O(d × (n + k))**, где d — цифры
7. Stack vs Queue? Stack LIFO, Queue FIFO
8. Стек на списке: push/pop O(1)? **ДА**
9. Очередь на списке: enqueue/dequeue O(1)? **ДА**
10. Heap — какая структура? **Полное бинарное дерево**
11. Buildheap O(n)? **ДА**, просеиваем вниз начиная с конца
12. Priority queue на heap? **ДА**, insert/extract O(log n)
13. Frog НВП? **НЕТ, это классическая DP**
14. Knapsack O(n × W)? **ДА**
15. LIS O(n log n)? **ДА, с бинарным поиском**

---

**Последний совет**: Понимай не только код, но и ПОЧЕМУ это работает. На коллоквиуме будут вопросы на объяснение, а не просто код.

Удачи! 🚀
